# vec2icon

Browser app that turns SVGs into an Apple Icon Composer `.icon` bundle.

- `pnpm i` — install dependencies
- `pnpm dev` — start the dev server
- `pnpm test` — run the test suite
- `pnpm build` — typecheck and build for production
