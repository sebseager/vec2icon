# Contributing

Contributions are welcome. Please open an issue first so we can talk through
the change before you spend time on a pull request.

Once it's agreed, fork the repo, make your change on a branch, and make sure
`pnpm lint`, `pnpm typecheck` and `pnpm test` pass before opening the PR.
